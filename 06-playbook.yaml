- name: variables from inventory files
  hosts: mongodb
  tasks:
  - name: print the variablesfrom inventory files
    ansible.builtin.debug:
     msg: the mongodb_username is {{MONGO_USERNAME}} , mongodb is {{MONGO_DB}}