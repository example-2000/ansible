- name: install nginx in host
  hosts: mongodb
  become: yes
  tasks:
   - name: install nginx
     ansible.builtin.yum:
      name: nginx
      state: installed
   - name: run the nginx
     ansible.builtin.service:
      name: nginx
      state: started